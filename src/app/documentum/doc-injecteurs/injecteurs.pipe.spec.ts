import { InjecteursPipe } from './injecteurs.pipe';

describe('InjecteursPipe', () => {
  it('create an instance', () => {
    const pipe = new InjecteursPipe();
    expect(pipe).toBeTruthy();
  });
});
